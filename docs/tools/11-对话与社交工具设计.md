# 对话与社交工具设计（Communication & Social Tools Design）

> 版本：v1.0
> 日期：2026-07-01
> 架构：LLM 调用工具 → 执行AI 执行操作 → 返回结果
> 协议：遵循 `00-工具协议规范.md` 统一接口契约

---

## 0. 设计原则

### 0.1 核心概念

**聊天操作由执行AI负责，LLM 只调用工具**

```
┌─────────────────────────────────────────────────────┐
│  LLM（大脑）                                         │
│  - 决定要发送消息/私聊/广播                           │
│  - 调用对话工具                                       │
└────────────────┬────────────────────────────────────┘
                 │ 调用工具
                 ▼
        ┌────────────────┐
        │  对话工具        │
        │  - chat         │
        │  - whisper      │
        │  - message      │
        └────────┬───────┘
                 │ 委托执行
                 ▼
        ┌────────────────┐
        │  执行AI（假人）  │
        │  - 发送消息     │
        │  - 监听消息     │
        └────────┬───────┘
                 │ 返回结果
                 ▼
        ┌────────────────┐
        │  工具返回        │
        │  - 成功/失败    │
        └────────────────┘
```

### 0.2 设计原则

| 原则 | 说明 |
|---|---|
| **简单直接** | 工具粒度适中，避免过度设计 |
| **有反馈** | 工具返回操作结果（成功/失败） |
| **可失败** | 玩家不在线、消息过长等 |

---

## 1. 工具列表

| 工具名 | 功能 | 参数 | 返回 |
|---|---|---|---|
| `chat` | 发送聊天消息 | `message: string`, `mode: 'chat' \| 'broadcast' \| 'emote'` | 操作结果 |
| `whisper` | 私聊 | `target: string`, `message: string` | 操作结果 |
| `message` | 消息管理 | `action: 'list' \| 'unread' \| 'mark_read' \| 'reply'`, `message_id?: string`, `content?: string` | 操作结果 |

---

## 2. 工具定义

### 2.1 chat（发送聊天消息）

通过 `mode` 参数支持普通聊天、广播、表情动作三种模式。

**ParameterSchema**

| 属性 | 类型 | 必填 | 描述 | 枚举值 |
|------|------|------|------|--------|
| `message` | `string` | 是 | 消息内容 | - |
| `mode` | `string` | 是 | 聊天模式 | `chat`, `broadcast`, `emote` |

**ResultEnvelope**

```typescript
ResultEnvelope<{}>  // 成功时无额外数据，失败时使用标准 ErrorCode
```

支持的错误码：`MESSAGE_TOO_LONG`（消息过长）、`MUTED`（被禁言）、`NO_PERMISSION`（无权限）、`EMOTE_NOT_FOUND`（表情动作不存在）、`INTERNAL_ERROR`（内部错误）

**示例调用**：

```
// 普通聊天
LLM 调用: chat(message="大家好！", mode="chat")
返回:
{
  "success": true,
  "data": {},
  "meta": { "duration": 100, "cost": { "hunger": 0.1 } }
}

// 广播（需要权限）
LLM 调用: chat(message="服务器将在 5 分钟后重启", mode="broadcast")
返回:
{
  "success": true,
  "data": {},
  "meta": { "duration": 100, "cost": { "hunger": 0.1 } }
}

// 表情动作
LLM 调用: chat(message="wave", mode="emote")
返回:
{
  "success": true,
  "data": {},
  "meta": { "duration": 80, "cost": { "hunger": 0.1 } }
}
```

### 2.2 whisper（私聊）

**ParameterSchema**

| 属性 | 类型 | 必填 | 描述 |
|------|------|------|------|
| `target` | `string` | 是 | 玩家名 |
| `message` | `string` | 是 | 消息内容 |

**ResultEnvelope**

```typescript
ResultEnvelope<{ target: string }>  // 成功时返回目标玩家名
```

支持的错误码：`PLAYER_NOT_FOUND`（玩家不在线）、`MESSAGE_TOO_LONG`（消息过长）、`INTERNAL_ERROR`（内部错误）

**示例调用**：

```
LLM 调用: whisper(target="Steve", message="你好，需要帮助吗？")
返回:
{
  "success": true,
  "data": { "target": "Steve" },
  "meta": { "duration": 100 }
}
```

**自然语言返回**：

```
私聊成功: 向 Steve 发送 "你好，需要帮助吗？"
```

### 2.3 message（消息管理）

通过 `action` 参数支持消息列表查询、未读消息、标记已读、回复消息四种操作。

**ParameterSchema**

| 属性 | 类型 | 必填 | 描述 | 枚举值 |
|------|------|------|------|--------|
| `action` | `string` | 是 | 操作类型 | `list`, `unread`, `mark_read`, `reply` |
| `message_id` | `string` | 否 | 消息ID（标记已读或回复时使用） | - |
| `content` | `string` | 否 | 回复内容（回复时使用） | - |
| `filter` | `object` | 否 | 过滤条件 | - |

`filter` 对象属性：

| 属性 | 类型 | 描述 |
|------|------|------|
| `sender` | `string` | 发送者 |
| `type` | `string` | 消息类型（`public`, `private`, `system`） |
| `keyword` | `string` | 关键词 |
| `since` | `number` | 起始时间戳 |
| `limit` | `number` | 返回数量上限 |

**ResultEnvelope**

```typescript
// action=list 或 action=unread 时
ResultEnvelope<{ messages: ChatMessage[]; total?: number; unreadCount?: number }>

// action=mark_read 或 action=reply 时
ResultEnvelope<{ replyTo?: string }>
```

支持的错误码：`MESSAGE_NOT_FOUND`（消息不存在）、`INTERNAL_ERROR`（内部错误）

**示例调用**：

```
// 获取消息列表
LLM 调用: message(action="list", filter={sender="Steve", limit=5})
返回:
{
  "success": true,
  "data": {
    "messages": [
      {
        "id": "msg_003",
        "sender": "Steve",
        "content": "帮我挖点钻石",
        "type": "public",
        "timestamp": 1719820800,
        "read": false
      }
    ],
    "total": 1
  },
  "meta": { "duration": 50 }
}

// 获取未读消息
LLM 调用: message(action="unread")
返回:
{
  "success": true,
  "data": {
    "messages": [
      {
        "id": "msg_001",
        "sender": "Steve",
        "content": "你好，需要帮助吗？",
        "type": "public",
        "timestamp": 1719820700,
        "read": false
      }
    ],
    "unreadCount": 1
  },
  "meta": { "duration": 50 }
}

// 标记已读
LLM 调用: message(action="mark_read", message_id="msg_001")
返回:
{
  "success": true,
  "data": {},
  "meta": { "duration": 30 }
}

// 回复消息
LLM 调用: message(action="reply", message_id="msg_001", content="好的，我这就去挖钻石")
返回:
{
  "success": true,
  "data": { "replyTo": "msg_001" },
  "meta": { "duration": 80 }
}
```

---

## 3. 消息队列系统

### 3.1 消息数据结构

```typescript
interface ChatMessage {
  id: string;
  sender: string;        // 发送者
  content: string;       // 消息内容
  type: 'public' | 'private' | 'system';  // 消息类型
  timestamp: number;     // 时间戳
  read: boolean;         // 是否已读
  replyTo?: string;      // 回复的消息ID
}
```

### 3.2 消息队列实现

```typescript
class MessageQueue {
  private messages: ChatMessage[] = [];
  private maxMessages: number = 100;  // 最多保留100条
  private contextSize: number = 20;   // 上下文窗口20条
  
  // 添加消息
  addMessage(msg: ChatMessage): void {
    this.messages.push(msg);
    // 超过上限时移除最旧的
    if (this.messages.length > this.maxMessages) {
      this.messages.shift();
    }
  }
  
  // 获取未读消息
  getUnread(limit?: number): ChatMessage[] {
    const unread = this.messages.filter(m => !m.read);
    return limit ? unread.slice(-limit) : unread;
  }
  
  // 按条件过滤
  getMessages(filter?: MessageFilter): ChatMessage[] {
    let result = [...this.messages];
    if (filter?.sender) result = result.filter(m => m.sender === filter.sender);
    if (filter?.type) result = result.filter(m => m.type === filter.type);
    if (filter?.keyword) result = result.filter(m => m.content.includes(filter.keyword!));
    if (filter?.since) result = result.filter(m => m.timestamp >= filter.since!);
    if (filter?.limit) result = result.slice(-filter.limit);
    return result;
  }
  
  // 标记已读
  markRead(messageId: string): boolean {
    const msg = this.messages.find(m => m.id === messageId);
    if (msg) {
      msg.read = true;
      return true;
    }
    return false;
  }
  
  // 获取上下文（最近N条）
  getContext(size?: number): ChatMessage[] {
    return this.messages.slice(-(size || this.contextSize));
  }
}
```

### 3.3 消息监听（执行AI）

```typescript
// 监听游戏内聊天事件
mc.onChat((player: Player, message: string) => {
  const chatMsg: ChatMessage = {
    id: `msg_${Date.now()}_${Math.random().toString(36).substr(2, 5)}`,
    sender: player.getRealName(),
    content: message,
    type: 'public',
    timestamp: Date.now(),
    read: false
  };
  messageQueue.addMessage(chatMsg);
});

// 监听私聊（通过命令响应解析）
mc.onCommandBlock((player, cmd) => {
  // 解析 /w 或 /msg 命令
  if (cmd.startsWith('w ') || cmd.startsWith('msg ')) {
    const parts = cmd.split(' ');
    const target = parts[1];
    const content = parts.slice(2).join(' ');
    
    if (target === agentPlayer.getRealName()) {
      const chatMsg: ChatMessage = {
        id: `msg_${Date.now()}_${Math.random().toString(36).substr(2, 5)}`,
        sender: player.getRealName(),
        content: content,
        type: 'private',
        timestamp: Date.now(),
        read: false
      };
      messageQueue.addMessage(chatMsg);
    }
  }
});

// 监听系统消息
mc.onServerStarted(() => {
  messageQueue.addMessage({
    id: `msg_${Date.now()}_sys`,
    sender: 'system',
    content: '服务器已启动',
    type: 'system',
    timestamp: Date.now(),
    read: false
  });
});
```

---

## 4. 工具实现

### 4.1 工具注册

```typescript
// 注册对话工具
function registerCommunicationTools() {
  // chat（支持 chat/broadcast/emote 三种模式）
  ToolRegistry.register({
    name: 'chat',
    description: '发送聊天消息，支持普通聊天、广播、表情动作三种模式',
    parameterSchema: {
      type: 'object',
      properties: {
        message: { type: 'string', description: '消息内容' },
        mode: { type: 'string', enum: ['chat', 'broadcast', 'emote'], description: '聊天模式' }
      },
      required: ['message', 'mode']
    },
    async invoke(params) {
      return await executionAI.chat(params.message, params.mode);
    }
  });
  
  // whisper
  ToolRegistry.register({
    name: 'whisper',
    description: '私聊',
    parameterSchema: {
      type: 'object',
      properties: {
        target: { type: 'string', description: '玩家名' },
        message: { type: 'string', description: '消息内容' }
      },
      required: ['target', 'message']
    },
    async invoke(params) {
      return await executionAI.whisper(params.target, params.message);
    }
  });
  
  // message（支持 list/unread/mark_read/reply 四种操作）
  ToolRegistry.register({
    name: 'message',
    description: '消息管理，支持消息列表查询、未读消息、标记已读、回复消息',
    parameterSchema: {
      type: 'object',
      properties: {
        action: { type: 'string', enum: ['list', 'unread', 'mark_read', 'reply'], description: '操作类型' },
        message_id: { type: 'string', description: '消息ID（标记已读或回复时使用）' },
        content: { type: 'string', description: '回复内容（回复时使用）' },
        filter: {
          type: 'object',
          description: '过滤条件',
          properties: {
            sender: { type: 'string', description: '发送者' },
            type: { type: 'string', enum: ['public', 'private', 'system'], description: '消息类型' },
            keyword: { type: 'string', description: '关键词' },
            since: { type: 'number', description: '起始时间戳' },
            limit: { type: 'number', description: '返回数量上限' }
          }
        }
      },
      required: ['action']
    },
    async invoke(params) {
      return await executionAI.message(params.action, params.message_id, params.content);
    }
  });
}
```

### 4.2 执行AI接口

```typescript
// 执行AI接口（对话操作）
interface ExecutionAI {
  chat(message: string, mode: 'chat' | 'broadcast' | 'emote'): Promise<ResultEnvelope>;
  whisper(target: string, message: string): Promise<ResultEnvelope>;
  message(action: string, message_id?: string, content?: string): Promise<ResultEnvelope>;
}
```

### 4.3 执行AI实现（示例）

```typescript
class ExecutionAIImpl implements ExecutionAI {
  private player: Player;
  
  constructor(player: Player) {
    this.player = player;
  }
  
  async chat(message: string, mode: 'chat' | 'broadcast' | 'emote'): Promise<ResultEnvelope> {
    switch (mode) {
      case 'chat':
        if (message.length > 256) {
          return {
            success: false,
            error: { code: 'MESSAGE_TOO_LONG', message: '消息过长，最多256字符' },
            meta: { duration: 10 }
          };
        }
        this.player.chat(message);
        return { success: true, data: {}, meta: { duration: 50 } };
        
      case 'broadcast':
        if (!this.player.isOp()) {
          return {
            success: false,
            error: { code: 'NO_PERMISSION', message: '无广播权限' },
            meta: { duration: 10 }
          };
        }
        if (message.length > 256) {
          return {
            success: false,
            error: { code: 'MESSAGE_TOO_LONG', message: '消息过长，最多256字符' },
            meta: { duration: 10 }
          };
        }
        mc.broadcast(message);
        return { success: true, data: {}, meta: { duration: 100 } };
        
      case 'emote':
        const validEmotes = ['wave', 'dance', 'laugh', 'cry', 'angry', 'point'];
        if (!validEmotes.includes(message)) {
          return {
            success: false,
            error: { code: 'EMOTE_NOT_FOUND', message: `表情动作不存在: ${message}` },
            meta: { duration: 10 }
          };
        }
        this.player.emote(message);
        return { success: true, data: {}, meta: { duration: 80 } };
    }
  }
  
  async whisper(target: string, message: string): Promise<ResultEnvelope> {
    const targetPlayer = mc.getPlayer(target);
    if (!targetPlayer) {
      return {
        success: false,
        error: { code: 'PLAYER_NOT_FOUND', message: `玩家不在线: ${target}` },
        meta: { duration: 20 }
      };
    }
    
    if (message.length > 256) {
      return {
        success: false,
        error: { code: 'MESSAGE_TOO_LONG', message: '消息过长，最多256字符' },
        meta: { duration: 10 }
      };
    }
    
    this.player.whisper(target, message);
    
    return { success: true, data: { target }, meta: { duration: 50 } };
  }
  
  async message(action: string, message_id?: string, content?: string): Promise<ResultEnvelope> {
    switch (action) {
      case 'list':
        const messages = messageQueue.getMessages();
        return { success: true, data: { messages, total: messages.length }, meta: { duration: 30 } };
        
      case 'unread':
        const unread = messageQueue.getUnread();
        return { success: true, data: { messages: unread, unreadCount: unread.length }, meta: { duration: 30 } };
        
      case 'mark_read':
        if (!message_id) {
          return {
            success: false,
            error: { code: 'INTERNAL_ERROR', message: '缺少 message_id 参数' },
            meta: { duration: 10 }
          };
        }
        const ok = messageQueue.markRead(message_id);
        if (!ok) {
          return {
            success: false,
            error: { code: 'MESSAGE_NOT_FOUND', message: `消息不存在: ${message_id}` },
            meta: { duration: 10 }
          };
        }
        return { success: true, data: {}, meta: { duration: 20 } };
        
      case 'reply':
        if (!message_id || !content) {
          return {
            success: false,
            error: { code: 'INTERNAL_ERROR', message: '缺少必要参数' },
            meta: { duration: 10 }
          };
        }
        this.player.chat(`/msg ${messageQueue.getMessage(message_id)?.sender} ${content}`);
        return { success: true, data: { replyTo: message_id }, meta: { duration: 80 } };
    }
  }
}
```

---

## 5. 完整流程示例

### 5.1 场景：公共聊天

```
[LLM 思考]
我需要告诉其他玩家我来了。

[LLM 调用工具]
chat(message="大家好！我是 AI 助手", mode="chat")

[工具返回]
发送成功: 大家好！我是 AI 助手
```

### 5.2 场景：私聊

```
[LLM 思考]
Steve 需要帮助，我私聊他。

[LLM 调用工具]
whisper(target="Steve", message="你好，需要我帮你挖矿吗？")

[工具返回]
私聊成功: 向 Steve 发送 "你好，需要我帮你挖矿吗？"
```

### 5.3 场景：广播

```
[LLM 思考]
服务器要重启了，我广播通知。

[LLM 调用工具]
chat(message="服务器将在 5 分钟后重启，请保存进度", mode="broadcast")

[工具返回]
广播成功: 服务器将在 5 分钟后重启，请保存进度
```

### 5.4 场景：表情动作

```
[LLM 思考]
完成任务了，我挥个手。

[LLM 调用工具]
chat(message="wave", mode="emote")

[工具返回]
表情成功: 挥手
```

---

## 6. 总结

### 6.1 核心设计

| 组件 | 职责 |
|---|---|
| **LLM（大脑）** | 决定要发送/接收消息，调用对话工具 |
| **对话工具** | 封装聊天操作（发送+接收），返回执行结果 |
| **消息队列** | 缓存消息，支持过滤/上下文/未读管理 |
| **执行AI（假人）** | 监听游戏事件，填充消息队列 |

### 6.2 关键特性

1. **双向通信**：发送（chat/whisper）+ 接收（message）
2. **消息队列**：缓存最近100条消息，支持按发送者/类型/关键词过滤
3. **上下文管理**：默认保留20条对话历史，支持对话连贯性
4. **有反馈**：工具返回操作结果（成功/失败）
5. **可失败**：玩家不在线、消息过长、无权限等

### 6.3 工具列表

| 工具名 | 功能 |
|---|---|
| `chat` | 发送聊天消息（支持普通聊天/广播/表情动作三种模式） |
| `whisper` | 私聊 |
| `message` | 消息管理（支持列表查询/未读消息/标记已读/回复消息四种操作） |

**总计：3 个工具**

### 6.4 消息相关性判断

AI 也是玩家，不是所有消息都与自己有关。需要判断消息相关性：

```typescript
interface MessageRelevance {
  relevance: 'high' | 'medium' | 'low' | 'none';  // 相关性等级
  reason: string;  // 判断原因
  shouldRespond: boolean;  // 是否应该回复
}

// 相关性判断规则
function judgeRelevance(message: ChatMessage, agentState: AgentState): MessageRelevance {
  // 高相关性：直接提到AI名字、私聊、询问AI
  if (message.content.includes(agentState.name) || message.type === 'private') {
    return { relevance: 'high', reason: '直接提及', shouldRespond: true };
  }
  
  // 中相关性：与当前任务相关、附近玩家聊天
  if (isRelatedToCurrentTask(message.content) || isNearbyPlayer(message.sender)) {
    return { relevance: 'medium', reason: '任务相关', shouldRespond: false };
  }
  
  // 低相关性：一般聊天、与AI无关
  return { relevance: 'low', reason: '与AI无关', shouldRespond: false };
}
```

**判断规则**：

| 相关性 | 条件 | 是否回复 |
|---|---|---|
| **high** | 直接提到AI名字、私聊、询问AI | 立即回复 |
| **medium** | 与当前任务相关、附近玩家聊天 | 延迟回复 |
| **low** | 一般聊天、与AI无关 | 不回复 |

### 6.5 消息时效性

消息有时效性，过期消息不需要处理：

```typescript
interface MessageTimeliness {
  isExpired: boolean;  // 是否过期
  age: number;  // 消息年龄（秒）
  urgency: 'urgent' | 'normal' | 'low';  // 紧急程度
}

// 时效性判断
function judgeTimeliness(message: ChatMessage): MessageTimeliness {
  const age = (Date.now() - message.timestamp) / 1000;  // 秒
  
  // 超过5分钟视为过期
  if (age > 300) {
    return { isExpired: true, age, urgency: 'low' };
  }
  
  // 超过1分钟紧急度降低
  if (age > 60) {
    return { isExpired: false, age, urgency: 'normal' };
  }
  
  return { isExpired: false, age, urgency: 'urgent' };
}
```

**时效性规则**：

| 年龄 | 紧急程度 | 处理方式 |
|---|---|---|
| < 1分钟 | urgent | 立即处理 |
| 1-5分钟 | normal | 正常处理 |
| > 5分钟 | expired | 不处理 |

### 6.6 选择性回复策略

不是所有消息都需要回复，需要决策：

```typescript
interface ResponseStrategy {
  shouldRespond: boolean;
  responsePriority: number;  // 0-100，越高越优先
  responseDelay: number;  // 延迟回复时间（毫秒）
  reason: string;
}

// 回复决策
function decideResponse(message: ChatMessage, agentState: AgentState): ResponseStrategy {
  const relevance = judgeRelevance(message, agentState);
  const timeliness = judgeTimeliness(message);
  
  // 过期消息不回复
  if (timeliness.isExpired) {
    return { shouldRespond: false, responsePriority: 0, responseDelay: 0, reason: '消息已过期' };
  }
  
  // 高相关性立即回复
  if (relevance.relevance === 'high') {
    return { shouldRespond: true, responsePriority: 90, responseDelay: 0, reason: '直接相关' };
  }
  
  // 中相关性延迟回复
  if (relevance.relevance === 'medium') {
    return { shouldRespond: true, responsePriority: 50, responseDelay: 2000, reason: '任务相关' };
  }
  
  // 低相关性不回复
  return { shouldRespond: false, responsePriority: 10, responseDelay: 0, reason: '与AI无关' };
}
```

### 6.7 消息优先级队列

使用优先级队列管理消息：

```typescript
class PriorityMessageQueue {
  private messages: Array<{
    message: ChatMessage;
    priority: number;
    timestamp: number;
  }> = [];
  
  // 添加消息（带优先级）
  addMessage(msg: ChatMessage, priority: number): void {
    this.messages.push({ message: msg, priority, timestamp: Date.now() });
    // 按优先级排序
    this.messages.sort((a, b) => b.priority - a.priority);
  }
  
  // 获取最高优先级消息
  getTopMessage(): ChatMessage | null {
    // 移除过期消息
    this.removeExpired();
    return this.messages.length > 0 ? this.messages[0].message : null;
  }
  
  // 移除过期消息
  private removeExpired(): void {
    const now = Date.now();
    this.messages = this.messages.filter(item => {
      const age = (now - item.message.timestamp) / 1000;
      return age < 300;  // 5分钟内有效
    });
  }
}
```

### 6.8 完整流程示例

```
[执行AI 收到消息]
Steve: "AI，帮我挖点钻石"
→ 相关性: high (直接提及)
→ 时效性: urgent (刚收到)
→ 优先级: 90
→ 加入优先级队列

[LLM 查询消息]
message(action="unread")
返回: Steve 的请求（优先级90）

[LLM 决策]
- 这是高相关性消息，需要回复
- 立即回复确认

[LLM 调用工具]
message(action="reply", message_id="msg_001", content="好的，我这就去挖钻石")

[执行AI 收到消息]
Alex: "今天天气不错"
→ 相关性: low (与AI无关)
→ 时效性: normal
→ 优先级: 10
→ 不回复

[执行AI 收到消息]
Bob: "谁有多余的铁锭？"
→ 相关性: medium (可能相关)
→ 时效性: normal
→ 优先级: 50
→ 延迟2秒后回复
```

### 6.9 下一步

- [ ] 实现执行AI接口（对话操作）
- [ ] 实现消息队列系统（含优先级）
- [ ] 实现消息监听机制
- [ ] 实现相关性判断逻辑
- [ ] 实现时效性判断逻辑
- [ ] 实现选择性回复策略
- [ ] 实现工具注册系统
- [ ] 测试完整流程

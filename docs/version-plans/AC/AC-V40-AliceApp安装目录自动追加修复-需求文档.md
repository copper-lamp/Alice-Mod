# AC-V40-AliceApp安装目录自动追加修复-需求文档

## 需求
- 安装程序在用户选择安装路径后，自动在末尾追加 `AliceApp`。
- 保持当前 NSIS 安装器行为不变，仅调整默认目录落点。
- 不影响已存在的升级、发布与打包流程。

## 目标
- 用户选择 `D:\Programs` 后，最终安装到 `D:\Programs\AliceApp`。
- 目录选择仍然可编辑，但最终落点统一规范化。

## 验收
- 安装器选择目录后，路径末尾自动补全 `AliceApp`。
- 生成的安装包可正常安装启动。

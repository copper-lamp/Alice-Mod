# McAgent 通信协议规范

> 版本：v1.0
> 日期：2026-07-04
> 范围：JSON 入口文件 · TCP 通信协议 · 工具注册 · 状态上报 · 数据库接口
> 关联文档：[00-顶层设计.md](../00-顶层设计.md)

---

## 第1章 概述

### 1.1 目的

本文档定义 McAgent 框架中 **Agent Core（智能体核心）** 与 **Adapter Core（接入核心/模组）** 之间的完整通信规范，包括：

- 实例发现与配置载入（JSON 入口文件）
- 实时通信协议（TCP JSON-RPC 2.0）
- 工具注册与调用流程
- 状态上报与事件通知
- 数据库接口规范

### 1.2 架构位置

```
┌────────────────────────────────────────────────────────────┐
│                        Agent Core                          │
│                                                           │
│  ┌──────────────┐  ┌──────────────┐  ┌───────────────┐    │
│  │ 实例管理器    │  │ TCP 服务端    │  │ 数据库读取器   │    │
│  │ (JSON入口)   │  │ (JSON-RPC)   │  │ (SQLite/Chroma)│    │
│  └──────┬───────┘  └──────┬───────┘  └───────┬───────┘    │
│         │                 │                  │             │
└─────────┼─────────────────┼──────────────────┼─────────────┘
          │ 读取 JSON       │ TCP 连接         │ 直接读取
          ▼                 ▼                  ▼
┌────────────────────────────────────────────────────────────┐
│                     Adapter Core                            │
│                                                             │
│  ┌──────────────┐  ┌──────────────┐  ┌───────────────┐    │
│  │ JSON 入口文件  │  │ TCP 客户端    │  │ 数据库文件     │    │
│  │ (instance.json)│  │ (JSON-RPC)   │  │ (SQLite/Chroma)│    │
│  └──────┬───────┘  └──────┬───────┘  └───────┬───────┘    │
│         │ 自动生成         │                 │             │
└─────────┼─────────────────┼──────────────────┼─────────────┘
          │                 │                  │
          ▼                 ▼                  ▼
     文件系统           TCP 网络            文件系统
```

### 1.3 关键术语

| 术语 | 说明 |
|------|------|
| **Agent Core** | 智能体核心桌面应用，管理多个实例 |
| **Adapter Core** | 接入核心，Minecraft 模组/插件 |
| **实例（Instance）** | 一个 Adapter Core 实例对应一个工作区 |
| **JSON 入口文件** | 模组自动生成的 JSON 文件，描述实例连接信息 |
| **工作区** | Agent Core 中对应一个实例的独立上下文 |

---

## 第2章 JSON 入口文件规范

### 2.1 设计目标

JSON 入口文件是 Adapter Core（模组）与 Agent Core 之间的 **实例发现与配置桥梁**，具有以下能力：

1. **不启动游戏也能管理**：Agent Core 读取入口文件后，可直接访问实例的数据库，进行离线配置编辑
2. **多实例管理**：每个模组实例生成独立的入口文件，Agent Core 通过文件列表管理多个实例
3. **配置持久化**：实例配置（LLM 模型、工具权限、角色设定等）存储在入口文件关联的数据库中
4. **多人协作**：入口文件路径约定明确，开发者可各自维护实例配置

### 2.2 文件位置与命名

```
# 模组生成路径（模组数据目录下自动生成）
<mod_data_dir>/mcagent_instance.json

# 典型路径示例（基岩版 LeviLamina）
plugins/McAdapter/mcagent_instance.json

# 典型路径示例（Java 版 Fabric）
config/mcagent/mcagent_instance.json
```

文件命名固定为 `mcagent_instance.json`，模组启动时自动生成/更新。

### 2.3 JSON Schema

```json
{
  "title": "McAgentInstance",
  "description": "McAgent 实例入口文件",
  "type": "object",
  "required": [
    "schema_version",
    "instance_id",
    "instance_name",
    "game_version",
    "mod_version",
    "status",
    "tcp",
    "auth",
    "database",
    "toolset_info"
  ],
  "properties": {
    "schema_version": {
      "type": "string",
      "description": "本规范的版本号，用于兼容性检查",
      "example": "1.0.0"
    },
    "instance_id": {
      "type": "string",
      "description": "实例唯一标识（UUID v4），模组首次启动时生成，持久化存储",
      "pattern": "^[0-9a-f]{8}-[0-9a-f]{4}-4[0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12}$"
    },
    "instance_name": {
      "type": "string",
      "description": "实例名称，默认使用服务器名，可在 Agent Core 中修改",
      "example": "生存服-主世界"
    },
    "game_version": {
      "type": "object",
      "description": "游戏版本信息",
      "required": ["edition", "version"],
      "properties": {
        "edition": {
          "type": "string",
          "enum": ["bedrock", "java"],
          "description": "游戏版本类型"
        },
        "version": {
          "type": "string",
          "description": "具体版本号",
          "example": "1.21.50"
        }
      }
    },
    "mod_version": {
      "type": "string",
      "description": "Adapter Core 模组版本号",
      "example": "1.0.0-alpha"
    },
    "status": {
      "type": "object",
      "description": "实例当前状态",
      "required": ["online", "last_online", "world_name"],
      "properties": {
        "online": {
          "type": "boolean",
          "description": "是否在线（TCP 连接状态）"
        },
        "last_online": {
          "type": "string",
          "format": "date-time",
          "description": "最后在线时间（ISO 8601）",
          "example": "2026-07-04T15:30:00+08:00"
        },
        "world_name": {
          "type": "string",
          "description": "当前世界名称",
          "example": "Bedrock level"
        }
      }
    },
    "tcp": {
      "type": "object",
      "description": "TCP 连接参数",
      "required": ["host", "port"],
      "properties": {
        "host": {
          "type": "string",
          "description": "Agent Core 连接地址（模组作为客户端主动连接）",
          "default": "127.0.0.1"
        },
        "port": {
          "type": "integer",
          "description": "Agent Core 监听端口",
          "minimum": 1024,
          "maximum": 65535,
          "default": 27541
        }
      }
    },
    "auth": {
      "type": "object",
      "description": "认证信息",
      "required": ["token"],
      "properties": {
        "token": {
          "type": "string",
          "description": "认证令牌，模组首次启动时随机生成，持久化存储",
          "example": "mct_8a3f7b2e9c1d5f6a"
        }
      }
    },
    "database": {
      "type": "object",
      "description": "数据库文件路径（Agent Core 可直接读取）",
      "required": ["sqlite_path", "chroma_path", "config_path"],
      "properties": {
        "sqlite_path": {
          "type": "string",
          "description": "SQLite 数据库文件绝对路径，存储结构化数据（记忆、配置等）",
          "example": "C:\\McServer\\plugins\\McAdapter\\data\\mcagent.db"
        },
        "chroma_path": {
          "type": "string",
          "description": "Chroma 向量数据库目录绝对路径，存储向量索引",
          "example": "C:\\McServer\\plugins\\McAdapter\\data\\chroma"
        },
        "config_path": {
          "type": "string",
          "description": "配置文件路径，存储实例运行配置",
          "example": "C:\\McServer\\plugins\\McAdapter\\config.json"
        },
        "log_path": {
          "type": "string",
          "description": "日志文件路径，可选",
          "example": "C:\\McServer\\plugins\\McAdapter\\logs"
        }
      }
    },
    "toolset_info": {
      "type": "object",
      "description": "工具集信息",
      "required": ["total_tools", "tool_categories"],
      "properties": {
        "total_tools": {
          "type": "integer",
          "description": "已注册工具总数",
          "example": 108
        },
        "tool_categories": {
          "type": "array",
          "description": "工具分类列表",
          "items": {
            "type": "object",
            "required": ["category", "count"],
            "properties": {
              "category": {
                "type": "string",
                "enum": ["perception", "movement", "inventory", "entity", "survival", "block", "chat", "qq", "memory", "task"]
              },
              "count": {
                "type": "integer",
                "description": "该类工具数量"
              }
            }
          }
        }
      }
    }
  }
}
```

### 2.4 完整示例

```json
{
  "schema_version": "1.0.0",
  "instance_id": "a1b2c3d4-e5f6-4789-abcd-ef1234567890",
  "instance_name": "生存服-主世界",
  "game_version": {
    "edition": "bedrock",
    "version": "1.21.50"
  },
  "mod_version": "1.0.0-alpha",
  "status": {
    "online": false,
    "last_online": "2026-07-04T15:30:00+08:00",
    "world_name": "Bedrock level"
  },
  "tcp": {
    "host": "127.0.0.1",
    "port": 27541
  },
  "auth": {
    "token": "mct_a1b2c3d4e5f67890abcdef1234567890"
  },
  "database": {
    "sqlite_path": "C:\\McServer\\plugins\\McAdapter\\data\\mcagent.db",
    "chroma_path": "C:\\McServer\\plugins\\McAdapter\\data\\chroma",
    "config_path": "C:\\McServer\\plugins\\McAdapter\\config.json",
    "log_path": "C:\\McServer\\plugins\\McAdapter\\logs"
  },
  "toolset_info": {
    "total_tools": 108,
    "tool_categories": [
      { "category": "perception", "count": 8 },
      { "category": "movement", "count": 9 },
      { "category": "inventory", "count": 10 },
      { "category": "entity", "count": 11 },
      { "category": "survival", "count": 6 },
      { "category": "block", "count": 7 },
      { "category": "chat", "count": 8 },
      { "category": "qq", "count": 13 },
      { "category": "memory", "count": 14 },
      { "category": "task", "count": 22 }
    ]
  }
}
```

### 2.5 Agent Core 读取流程

```
用户点击 "添加实例" → 打开文件选择对话框（过滤 .json）
    ↓
选择 mcagent_instance.json
    ↓
Agent Core 读取文件：
  1. 校验 schema_version 兼容性
  2. 读取 instance_id，检查是否已存在（防止重复添加）
  3. 读取 auth.token，准备后续 TCP 连接认证
  4. 读取 database 路径，建立数据库连接
    ↓
创建新工作区（Workspace）：
  - 工作区 ID = instance_id
  - 工作区名称 = instance_name
  - 显示连接状态（online/offline）
  - 可用时自动尝试 TCP 连接
    ↓
用户可以离线操作：
  - 浏览/编辑数据库中的记忆
  - 修改配置文件（LLM 模型、工具权限、角色设定）
  - 查看日志
```

### 2.6 文件生命周期

| 阶段 | 操作 | 说明 |
|------|------|------|
| 模组首次启动 | 生成 JSON 文件 | 生成 instance_id、auth.token，初始化数据库 |
| 模组每次启动 | 更新 JSON 文件 | 更新 status、tcp 端口、world_name |
| 模组运行时 | 保持 JSON 文件最新 | TCP 连接状态变化时更新 online 字段 |
| 模组关闭 | 更新 JSON 文件 | 设置 online=false，记录 last_online |
| 配置变更 | 可写入 config_path | LLM 模型、工具权限等变更持久化到 config.json |

---

## 第3章 TCP 通信协议

### 3.1 协议栈

| 层 | 协议 | 说明 |
|----|------|------|
| 传输层 | TCP | 长连接，粘包处理 |
| 应用层 | JSON-RPC 2.0 | 请求/响应/通知/Batch |
| 序列化 | JSON（UTF-8） | 每次消息一行，`\n` 分隔 |
| 安全 | 认证令牌 | 握手时验证 |

### 3.2 消息帧格式

每条消息独立一行，以 `\n`（换行符，0x0A）作为消息边界。

```
{ "jsonrpc": "2.0", "id": 1, "method": "tool_call", "params": {...} }\n
{ "jsonrpc": "2.0", "id": 1, "result": { "success": true, ... } }\n
```

**粘包处理规则**：

- 发送方：每条 JSON 消息后追加 `\n`
- 接收方：按 `\n` 分割字节流，逐条解析 JSON
- 批量调用：整个 JSON 数组作为一条消息，数组后追加 `\n`

### 3.3 连接建立流程

```
Adapter Core                         Agent Core
    │                                    │
    │  1. TCP 连接                       │
    │ ──────────────────────────────→    │
    │                                    │
    │  2. 握手请求 (hello)               │
    │ ──────────────────────────────→    │
    │                                    │
    │  3. 握手响应 (hello_result)        │
    │ ←──────────────────────────────    │
    │                                    │
    │  4. 工具注册 (register_tools)      │
    │ ──────────────────────────────→    │
    │                                    │
    │  5. 注册确认 (register_tools_ack)  │
    │ ←──────────────────────────────    │
    │                                    │
    │  6. 开始心跳 (ping/pong)           │
    │ ◄══════════════════════════════►   │
    │                                    │
    │  7. 进入正常通信                   │
    │ ◄══════════════════════════════►   │
```

### 3.4 握手协议

#### 握手请求（Adapter Core → Agent Core）

```json
{
  "jsonrpc": "2.0",
  "id": 1,
  "method": "hello",
  "params": {
    "instance_id": "a1b2c3d4-e5f6-4789-abcd-ef1234567890",
    "schema_version": "1.0.0",
    "auth_token": "mct_a1b2c3d4e5f67890abcdef1234567890",
    "game_version": {
      "edition": "bedrock",
      "version": "1.21.50"
    },
    "mod_version": "1.0.0-alpha"
  }
}
```

#### 握手响应（Agent Core → Adapter Core）

成功：

```json
{
  "jsonrpc": "2.0",
  "id": 1,
  "result": {
    "success": true,
    "session_id": "sess_abc123",
    "server_version": "1.0.0",
    "heartbeat_interval": 10
  }
}
```

失败：

```json
{
  "jsonrpc": "2.0",
  "id": 1,
  "error": {
    "code": -32001,
    "message": "Authentication failed",
    "data": {
      "reason": "invalid_token"
    }
  }
}
```

### 3.5 标准消息格式

#### 请求

```json
{
  "jsonrpc": "2.0",
  "id": 1,
  "method": "method_name",
  "params": { }
}
```

#### 成功响应

```json
{
  "jsonrpc": "2.0",
  "id": 1,
  "result": {
    "success": true,
    "data": { },
    "duration_ms": 1234
  }
}
```

#### 失败响应

```json
{
  "jsonrpc": "2.0",
  "id": 1,
  "error": {
    "code": -32000,
    "message": "Error description",
    "data": {
      "reason": "error_reason",
      "detail": "详细错误信息"
    }
  }
}
```

#### 通知（无响应）

```json
{
  "jsonrpc": "2.0",
  "method": "notification_method",
  "params": { }
}
```

### 3.6 方法列表

#### 客户端 → 服务端（Adapter Core → Agent Core）

| 方法 | 类型 | 方向 | 说明 |
|------|------|------|------|
| `hello` | 请求 | C→S | 握手认证 |
| `register_tools` | 请求 | C→S | 注册工具列表 |
| `update_tools` | 请求 | C→S | 更新工具列表 |
| `status_report` | 通知 | C→S | 状态上报 |
| `event` | 通知 | C→S | 事件通知 |
| `pong` | 通知 | C→S | 心跳回复 |

#### 服务端 → 客户端（Agent Core → Adapter Core）

| 方法 | 类型 | 方向 | 说明 |
|------|------|------|------|
| `tool_call` | 请求 | S→C | 调用工具 |
| `tool_call_batch` | 请求 | S→C | 批量调用工具 |
| `ping` | 通知 | S→C | 心跳检测 |
| `config_update` | 通知 | S→C | 配置更新推送 |

### 3.7 工具调用协议

#### 单个工具调用

```json
// Agent Core → Adapter Core
{
  "jsonrpc": "2.0",
  "id": 10,
  "method": "tool_call",
  "params": {
    "tool_name": "move_to",
    "parameters": {
      "x": 100,
      "y": 64,
      "z": 200,
      "dimension": "overworld"
    },
    "timeout_ms": 30000
  }
}

// Adapter Core → Agent Core
{
  "jsonrpc": "2.0",
  "id": 10,
  "result": {
    "success": true,
    "data": {
      "position": { "x": 100, "y": 64, "z": 200 },
      "duration_ms": 3450,
      "hunger_cost": 2
    },
    "duration_ms": 3450
  }
}
```

#### 批量工具调用

```json
// Agent Core → Adapter Core
[
  {
    "jsonrpc": "2.0",
    "id": 11,
    "method": "tool_call",
    "params": {
      "tool_name": "move_to",
      "parameters": { "x": 100, "y": 64, "z": 200 },
      "timeout_ms": 30000
    }
  },
  {
    "jsonrpc": "2.0",
    "id": 12,
    "method": "tool_call",
    "params": {
      "tool_name": "mine_block",
      "parameters": { "x": 100, "y": 63, "z": 200 },
      "timeout_ms": 10000
    }
  }
]

// Adapter Core → Agent Core
[
  {
    "jsonrpc": "2.0",
    "id": 11,
    "result": { "success": true, "data": { ... }, "duration_ms": 3450 }
  },
  {
    "jsonrpc": "2.0",
    "id": 12,
    "result": { "success": true, "data": { ... }, "duration_ms": 2300 }
  }
]
```

**执行规则**：

1. Adapter Core 收到 Batch 后，分析工具间的依赖关系
2. **无依赖**：并行执行所有工具
3. **有依赖**：按依赖关系顺序执行（如 `move_to` 是 `mine_block` 的前提）
4. 依赖关系由工具名称语义推断（`move_to`/`move_to_entity` 等移动类工具天然是后续操作的前提）
5. **统一返回**：所有工具执行完毕后，一次性返回结果数组（顺序与请求一致）

---

## 第4章 工具注册协议

### 4.1 注册工具

Adapter Core 连接建立后，立即发送 `register_tools` 注册其实现的工具列表。

```json
{
  "jsonrpc": "2.0",
  "id": 2,
  "method": "register_tools",
  "params": {
    "tools": [
      {
        "name": "move_to",
        "description": "走到指定坐标",
        "category": "movement",
        "input_schema": {
          "type": "object",
          "properties": {
            "x": { "type": "number", "description": "目标 X 坐标" },
            "y": { "type": "number", "description": "目标 Y 坐标" },
            "z": { "type": "number", "description": "目标 Z 坐标" },
            "dimension": {
              "type": "string",
              "enum": ["overworld", "nether", "end"],
              "default": "overworld",
              "description": "维度"
            }
          },
          "required": ["x", "y", "z"]
        },
        "output_schema": {
          "type": "object",
          "properties": {
            "position": {
              "type": "object",
              "properties": {
                "x": { "type": "number" },
                "y": { "type": "number" },
                "z": { "type": "number" }
              }
            },
            "duration_ms": { "type": "number" },
            "hunger_cost": { "type": "number" }
          }
        },
        "execution": {
          "timeout_default_ms": 30000,
          "timeout_max_ms": 120000,
          "is_movement": true,
          "is_async": false
        }
      }
    ]
  }
}
```

#### 工具注册参数说明

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `name` | string | 是 | 工具名称，全局唯一，snake_case |
| `description` | string | 是 | 工具描述，LLM 理解用途 |
| `category` | string | 是 | 工具分类：perception/movement/inventory/entity/survival/block/chat/qq/memory/task |
| `input_schema` | object | 是 | 参数 JSON Schema，用于 LLM Function Calling |
| `output_schema` | object | 否 | 返回值 JSON Schema，可选但建议提供 |
| `execution.timeout_default_ms` | number | 否 | 默认超时时间（毫秒） |
| `execution.timeout_max_ms` | number | 否 | 最大超时时间 |
| `execution.is_movement` | boolean | 否 | 是否为移动类工具（影响依赖分析） |
| `execution.is_async` | boolean | 否 | 是否为异步工具（无需等待结果） |

### 4.2 注册确认

```json
{
  "jsonrpc": "2.0",
  "id": 2,
  "result": {
    "success": true,
    "data": {
      "registered_count": 108,
      "tool_names": ["move_to", "move_to_entity", ...]
    },
    "duration_ms": 5
  }
}
```

### 4.3 更新工具列表

运行时更新工具列表（如热更新插件后）：

```json
{
  "jsonrpc": "2.0",
  "id": 5,
  "method": "update_tools",
  "params": {
    "tools": [ ... ],
    "mode": "replace"
  }
}
```

`mode` 取值：

| 值 | 说明 |
|----|------|
| `replace` | 完全替换整个工具列表 |
| `merge` | 合并新增/更新已有工具 |
| `remove` | 移除指定工具（需提供 `tools` 中的 `name` 数组） |

---

## 第5章 状态上报协议

### 5.1 周期性状态上报

Adapter Core 定期（默认每 2 秒）上报基础状态，使用通知方式（无需响应）。

```json
{
  "jsonrpc": "2.0",
  "method": "status_report",
  "params": {
    "timestamp": "2026-07-04T15:30:05.123+08:00",
    "health": {
      "health": 18,
      "max_health": 20,
      "hunger": 15,
      "max_hunger": 20,
      "saturation": 3.2,
      "air": 300,
      "max_air": 300
    },
    "position": {
      "x": 128.5,
      "y": 64.0,
      "z": -256.3,
      "dimension": "overworld",
      "yaw": 45.0,
      "pitch": 30.0
    },
    "defense": {
      "armor": 12,
      "armor_toughness": 2
    },
    "equipment": {
      "mainhand": "iron_sword",
      "offhand": "shield",
      "helmet": "iron_helmet",
      "chestplate": "iron_chestplate",
      "leggings": "iron_leggings",
      "boots": "iron_boots"
    },
    "inventory_summary": {
      "used_slots": 23,
      "total_slots": 36,
      "item_count": 15,
      "items": [
        { "name": "apple", "count": 12 },
        { "name": "cobblestone", "count": 45 }
      ]
    },
    "status_effects": [],
    "world_info": {
      "time": 6000,
      "weather": "clear",
      "difficulty": "normal",
      "game_mode": "survival"
    }
  }
}
```

### 5.2 事件通知

当发生重要事件时，Adapter Core 主动推送事件通知。

```json
{
  "jsonrpc": "2.0",
  "method": "event",
  "params": {
    "event_type": "entity_attack",
    "severity": "warning",
    "timestamp": "2026-07-04T15:30:10.456+08:00",
    "data": {
      "attacker": {
        "type": "zombie",
        "id": "zombie_123",
        "distance": 5.2
      },
      "damage": 3,
      "health_after": 15
    }
  }
}
```

#### 事件类型列表

| 事件类型 | 严重度 | 说明 |
|----------|--------|------|
| `entity_attack` | warning | 被实体攻击 |
| `health_low` | danger | 血量过低（< 5） |
| `hunger_low` | warning | 饥饿值过低（< 6） |
| `player_chat` | info | 玩家发送聊天消息 |
| `player_join` | info | 玩家加入游戏 |
| `player_leave` | info | 玩家离开游戏 |
| `death` | danger | AI 死亡 |
| `tool_broken` | warning | 工具损坏 |
| `inventory_full` | info | 背包已满 |
| `environment_change` | info | 环境变化（天气/时间） |
| `task_completed` | info | 任务完成 |

---

## 第6章 心跳与重连协议

### 6.1 心跳机制

```
Agent Core                          Adapter Core
    │                                    │
    │  ping (服务端发送)                  │
    │ ──────────────────────────────→    │
    │                                    │
    │  pong (客户端回复)                  │
    │ ←──────────────────────────────    │
    │                                    │
    │  (每 10 秒重复)                    │
    │ ◄══════════════════════════════►   │
```

#### Ping（Agent Core → Adapter Core）

```json
{
  "jsonrpc": "2.0",
  "method": "ping",
  "params": {
    "timestamp": "2026-07-04T15:30:20.000+08:00"
  }
}
```

#### Pong（Adapter Core → Agent Core）

```json
{
  "jsonrpc": "2.0",
  "method": "pong",
  "params": {
    "timestamp": "2026-07-04T15:30:20.001+08:00",
    "tick": 1234567
  }
}
```

### 6.2 超时与重连

| 参数 | 值 | 说明 |
|------|-----|------|
| 心跳间隔 | 10 秒 | 服务端每隔 10 秒发送 ping |
| 超时阈值 | 30 秒 | 超过 30 秒未收到 pong 视为断开 |
| 最大重连次数 | 5 次 | 尝试重连的最大次数 |
| 重连间隔 | 1s, 2s, 4s, 8s, 16s | 指数退避策略 |
| 重连超时 | 60 秒 | 总重连超时时间 |

**重连流程**：

```
Agent Core 检测到超时
    ↓
标记实例为 offline
    ↓
更新 JSON 入口文件: status.online = false
    ↓
启动重连计时器（指数退避）
    ↓
┌─ 重连循环（最多 5 次）─────────────┐
│  等待 1s/2s/4s/8s/16s             │
│  → 尝试连接                       │
│  → 成功？是 → 握手 → 工具注册 → 恢复 │
│  → 失败？继续等待                  │
└────────────────────────────────────┘
    ↓
所有重连失败 → 保持 offline 状态
    ↓
用户手动重连或等待下一次启动
```

**重连成功后的恢复流程**：

```
1. TCP 连接建立
2. 发送 hello（携带 instance_id 和 auth_token）
3. 服务端验证通过，返回 session_id
4. 重新注册工具列表（register_tools）
5. 恢复状态同步（发送最新 status_report）
6. 恢复心跳
7. 标记实例为 online
```

---

## 第7章 数据库接口规范

### 7.1 概述

Agent Core 可直接通过 JSON 入口文件中的 `database` 路径访问实例的数据库文件，实现：

- **离线配置编辑**：不启动游戏即可修改 LLM 模型、工具权限等
- **记忆浏览与管理**：查看/编辑/删除记忆，无需通过工具调用
- **日志查看**：读取实例运行日志

### 7.2 SQLite 数据库表结构

#### 表：`config`

实例配置表，存储键值对配置。

```sql
CREATE TABLE config (
  key TEXT PRIMARY KEY,
  value TEXT NOT NULL,
  value_type TEXT NOT NULL DEFAULT 'string',  -- string, number, boolean, json
  updated_at INTEGER NOT NULL DEFAULT (unixepoch()),
  description TEXT
);
```

**预定义配置键**：

| key | value_type | 默认值 | 说明 |
|-----|-----------|--------|------|
| `instance_name` | string | "McAgent" | 实例名称 |
| `llm_provider` | string | "openai" | LLM Provider 类型 |
| `llm_model` | string | "gpt-4o" | LLM 模型名称 |
| `llm_api_key` | string | "" | API Key |
| `llm_base_url` | string | "" | 自定义 Base URL |
| `llm_temperature` | number | 0.7 | 模型温度参数 |
| `llm_max_tokens` | number | 4096 | 最大输出 Token 数 |
| `tool_permissions` | json | "*" | 允许的工具列表（"*" 表示全部） |
| `heartbeat_interval` | number | 10 | 心跳间隔（秒） |
| `auto_reconnect` | boolean | true | 是否自动重连 |
| `language` | string | "zh-CN" | Agent 语言 |
| `agent_persona` | json | {} | 角色设定 |

#### 表：`memory_meta`

记忆元数据表。

```sql
CREATE TABLE memory_meta (
  id TEXT PRIMARY KEY,                            -- UUID
  type TEXT NOT NULL,                             -- player_habit, map_point, task_experience, social, skill
  tags TEXT DEFAULT '[]',                         -- JSON array ["tag1", "tag2"]
  importance INTEGER NOT NULL DEFAULT 5,          -- 1-10
  access_count INTEGER NOT NULL DEFAULT 0,
  created_at INTEGER NOT NULL DEFAULT (unixepoch()),
  updated_at INTEGER NOT NULL DEFAULT (unixepoch()),
  expires_at INTEGER,                             -- NULL 表示永不过期
  content_json TEXT NOT NULL,                     -- 记忆内容 JSON 字符串
  embedding_id TEXT                               -- Chroma 向量 ID（关联用）
);

CREATE INDEX idx_memory_meta_type ON memory_meta(type);
CREATE INDEX idx_memory_meta_importance ON memory_meta(importance);
CREATE INDEX idx_memory_meta_created_at ON memory_meta(created_at);
```

#### 表：`memory_tags`

标签索引表（加速标签检索）。

```sql
CREATE TABLE memory_tags (
  memory_id TEXT NOT NULL,
  tag TEXT NOT NULL,
  PRIMARY KEY (memory_id, tag),
  FOREIGN KEY (memory_id) REFERENCES memory_meta(id) ON DELETE CASCADE
);

CREATE INDEX idx_memory_tags_tag ON memory_tags(tag);
```

#### 表：`task_archive`

任务存档表。

```sql
CREATE TABLE task_archive (
  id TEXT PRIMARY KEY,
  name TEXT NOT NULL,
  description TEXT,
  type TEXT NOT NULL,                             -- simple, composite, loop, conditional
  status TEXT NOT NULL DEFAULT 'pending',
  priority TEXT NOT NULL DEFAULT 'normal',
  progress INTEGER NOT NULL DEFAULT 0,
  parent_id TEXT,                                 -- 父任务 ID（复合任务）
  dependencies TEXT DEFAULT '[]',                 -- JSON array 依赖任务 ID 列表
  result_json TEXT,                               -- 执行结果 JSON
  error TEXT,                                     -- 错误信息
  created_at INTEGER NOT NULL DEFAULT (unixepoch()),
  started_at INTEGER,
  completed_at INTEGER,
  updated_at INTEGER NOT NULL DEFAULT (unixepoch())
);

CREATE INDEX idx_task_archive_status ON task_archive(status);
CREATE INDEX idx_task_archive_parent ON task_archive(parent_id);
```

#### 表：`logs`

运行日志表。

```sql
CREATE TABLE logs (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  level TEXT NOT NULL,                            -- debug, info, warning, error
  source TEXT NOT NULL,                           -- 日志来源模块
  message TEXT NOT NULL,
  data_json TEXT,                                 -- 附加数据 JSON
  created_at INTEGER NOT NULL DEFAULT (unixepoch())
);

CREATE INDEX idx_logs_level ON logs(level);
CREATE INDEX idx_logs_created_at ON logs(created_at);
```

### 7.3 配置文件规范

`config.json` 文件存储实例运行配置，与 SQLite 中的 `config` 表结构一致，供游戏内直接读取。

```json
{
  "schema_version": "1.0.0",
  "instance_name": "生存服-主世界",
  "llm": {
    "provider": "openai",
    "model": "gpt-4o",
    "api_key": "",
    "base_url": "",
    "temperature": 0.7,
    "max_tokens": 4096
  },
  "tool_permissions": {
    "mode": "allow_all",
    "allowed_tools": [],
    "blocked_tools": [],
    "categories": ["perception", "movement", "inventory", "entity", "survival", "block", "chat", "qq", "memory", "task"]
  },
  "agent": {
    "language": "zh-CN",
    "persona": {
      "name": "Alice",
      "description": "一个乐于助人的 Minecraft AI 助手"
    }
  },
  "heartbeat_interval": 10,
  "auto_reconnect": true
}
```

### 7.4 数据库访问规则

| 场景 | 访问方 | 方式 | 说明 |
|------|--------|------|------|
| Agent Core 离线管理 | Agent Core | 直接 SQLite 读取 | 游戏未启动时修改配置、查看记忆 |
| Agent Core 运行时 | Agent Core | 通过工具调用 | 通过 memory_* 工具操作记忆 |
| Adapter Core 运行时 | Adapter Core | 直接 SQLite 写入 | 存储状态、日志、记忆 |
| 冲突解决 | 双方 | 写操作加锁 | 使用 WAL 模式，避免同时写同一表 |

---

## 第8章 错误码表

### 8.1 JSON-RPC 标准错误码

| 错误码 | 含义 | 说明 |
|--------|------|------|
| `-32700` | Parse Error | JSON 解析错误 |
| `-32600` | Invalid Request | 请求格式错误 |
| `-32601` | Method Not Found | 方法不存在 |
| `-32602` | Invalid Params | 参数校验失败 |
| `-32603` | Internal Error | 内部错误 |

### 8.2 自定义错误码

| 错误码 | 含义 | 说明 |
|--------|------|------|
| `-32000` | Tool Execution Failed | 工具执行失败 |
| `-32001` | Authentication Failed | 认证失败（token 无效/过期） |
| `-32002` | Not Authenticated | 未认证（先发 hello） |
| `-32003` | Schema Version Mismatch | 协议版本不兼容 |
| `-32004` | Tool Not Found | 工具未注册 |
| `-32005` | Tool Timeout | 工具执行超时 |
| `-32006` | Instance Busy | 实例忙（正在执行其他工具） |
| `-32007` | Game Not Ready | 游戏未就绪（世界未加载） |
| `-32008` | Rate Limited | 调用频率过高 |
| `-32009` | Connection Limit | 连接数超限 |

### 8.3 错误响应示例

```json
{
  "jsonrpc": "2.0",
  "id": 1,
  "error": {
    "code": -32000,
    "message": "Tool execution failed",
    "data": {
      "reason": "path_not_found",
      "detail": "目标位置不可达：被岩浆湖阻挡",
      "position": { "x": 50, "y": 64, "z": 100 },
      "suggestion": "尝试绕路或选择其他目标"
    }
  }
}
```

---

## 第9章 版本兼容性

### 9.1 版本号规则

使用 **语义化版本号**（SemVer）：`MAJOR.MINOR.PATCH`

| 版本位 | 变更 | 说明 |
|--------|------|------|
| MAJOR | 不兼容的协议变更 | 需要同时升级 Agent Core 和 Adapter Core |
| MINOR | 向下兼容的功能新增 | 可单独升级一方 |
| PATCH | 向下兼容的 Bug 修复 | 可独立升级 |

### 9.2 兼容性检查

1. 握手时双方交换 `schema_version`
2. Agent Core 检查 `MAJOR` 版本是否一致
3. 不一致时拒绝连接，返回错误码 `-32003`
4. `MINOR` 版本不同时，以较低版本为准，限制功能

### 9.3 文件变更记录

| 版本 | 日期 | 变更内容 |
|------|------|----------|
| 1.0.0 | 2026-07-04 | 初始版本 |

---

## 附录：快速参考

### 通信流程时序图

```
时间  Adapter Core                    Agent Core                 用户
 │       │                               │                        │
 │       │─── TCP 连接 ─────────────────→│                        │
 │       │─── hello ───────────────────→│                        │
 │       │←── hello_result ────────────│                        │
 │       │─── register_tools ──────────→│                        │
 │       │←── register_tools_ack ──────│                        │
 │       │                               │                        │
 │       │  === 心跳循环 ===             │                        │
 │       │←── ping ────────────────────│                        │
 │       │─── pong ───────────────────→│                        │
 │       │                               │                        │
 │       │  === 工具调用 ===             │                        │
 │       │                               │←── 用户输入/QQ消息 ──│
 │       │                               │  (LLM 决策生成工具调用) │
 │       │←── tool_call (Batch) ───────│                        │
 │       │─── result ─────────────────→│                        │
 │       │                               │  (LLM 继续决策)       │
 │       │                               │──→ 输出结果 ──────────│
 │       │                               │                        │
 │       │  === 状态上报 ===             │                        │
 │       │─── status_report (通知) ────→│                        │
 │       │─── event (通知) ────────────→│                        │
 │       │                               │                        │
```

### 工具调用错误码速查

| 错误码 | 含义 | 常见场景 |
|--------|------|----------|
| -32000 | 执行失败 | 路径不可达、物品不足、方块不存在 |
| -32001 | 认证失败 | token 错误或过期 |
| -32004 | 工具不存在 | 调用了未注册的工具 |
| -32005 | 执行超时 | 寻路超时、战斗超时 |
| -32007 | 游戏未就绪 | 世界未完全加载 |
{
  "name": "@alice-mod/adapter-bedrock",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "tsc && node scripts/copy-to-bds.js",
    "dev": "tsc && node scripts/copy-to-bds.js && tsc --watch",
    "typecheck": "tsc --noEmit",
    "test": "echo 'No tests yet'",
    "copy-bds": "node scripts/copy-to-bds.js"
  },
  "devDependencies": {
    "@types/node": "^22.20.0",
    "typescript": "^5.4.0"
  }
}
